Server:
  Config changes:
    ☐ logback.xml - send logs to console @hardcode
        possibly request-logging.xml as well
    ☐ ca.cfg - option to disable CA service @environment
    ☐ puppetserver.conf - option to set protocol and cipher suites @environment
    ☐ webserver.conf - option to set server port number @environment

  Containers:
    Volumes:
      vardir = /opt/puppetlabs/server/data/puppetserver
      logdir = /var/log/puppetlabs/puppetserver
      rundir = /var/run/puppetlabs/puppetserver
      pidfile = /var/run/puppetlabs/puppetserver/puppetserver.pid
      codedir = /etc/puppetlabs/code

Settings from environment variables:
  Server:
    ✔ DNS_ALT_NAMES
    ☐ CA_SERVER
    ☐ REPORT_SERVER
    ☐ JAVA_ARGS
      * Default "-Xms2g -Xmx2g -Djruby.logger.class=com.puppetlabs.jruby_utils.jruby.Slf4jLogger"
    ☐ JRUBY_JAR  ( )
  Agent:
    ☐ PUPPET_RUN_INTERVAL
